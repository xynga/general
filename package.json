{
  "name": "xynga-general",
  "version": "1.1.4",
  "description": "Holds all the components classified under the label \"General\"",
  "main": "dist/inline/index.js",
  "types": "dist/inline/index.d.ts",
  "files": [
    "dist/inline"
  ],
  "scripts": {
    "build": "cd src && npm run compile",
    "link-build": "npm link && cd testing && npm link xynga-general",
    "postversion": "git push",
    "rebuild": "npm run build && npm start",
    "setup": "cd src && npm i && cd .. && npm run build && cd testing && npm i && cd .. && npm run link-build",
    "start": "cd testing && start chrome http://localhost:4200 && ng serve",
    "test": "npm run start"
  },
  "author": "GradTech V",
  "license": "MIT",
  "peerDependencies": {
    "@angular/animations": ">=4.0.0",
    "@angular/core": ">=4.0.0",
    "@angular/common": ">=4.0.0",
    "@angular/platform-browser": ">=4.0.0",
    "prismjs": ">=1.8.3"
  },
  "dependencies": {
    "angular2-notifications": "^0.7.8"
  }
}
